# TRAPS Journal Template

Use each section to capture: the wrong answer first considered, the right answer, and a proof link (commit/PR).

## 1. Spring Boot API with in-memory transfer list

## 2. PostgreSQL persistence, Flyway migrations, @Transactional

## 3. Validation, global exception handling, JWT/API-key protection

## 4. Redis cache for GET /transfers/{id} and rate limiting

## 5. Kafka producer plus an audit consumer

## 6. Reconciliation job comparing transfers against audit events, flagging discrepancies

## 7. Docker Compose for the entire local system

## 8. Docker image -> ECR -> ECS Fargate, connected to RDS

## 9. Actuator health checks and CloudWatch logs

## 10. (Stretch) Kubernetes manifests, local kind/minikube deploy
